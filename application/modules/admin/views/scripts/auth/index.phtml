<form class="" method="post" enctype="multipart/form-data">
<div class="container">
    <div class="row">
        <div class="alert-message warning span5 offset4" id="hint">            
            
            <table>
                <tr>
                    <td colspan="2">
                        <h3>BIENVENIDO</h3><br>
                        
                        
                        <h5>    <span>Estás tratando de acceder a un lugar Restringido <br>
                                Ingresa tus datos de acceso
                            </h5>
                            </span>
                    </td>
                </tr>
                <tr>
                    <td>Email : </td>
                    <td><input type="text" name="email" required="required"/> </td> 
                </tr>
                <tr>
                    <td>Password : </td>
                    <td><input type="password" name="clave" required="required"/></td>
                </tr>
                <tr style="text-align: center">
                    <td colspan="2"><input type="submit" class="btn" name="ingresar" value="INGRESAR"/></td>
                </tr>
            </table>
        </div>
    </div>
</div>
</form>
<div id="fmsgs">
    <? if($this->fmsgs):
        foreach ($this->fmsgs as $msg): ?>
        <div class="alert <?= $msg->level == 'debug' ? 'error block-message' : $msg->level ?>">           
            <?= $msg->message ?>
        </div>            
    <? endforeach; endif;?>
</div>
